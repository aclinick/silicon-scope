<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="silicon_scope.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:silicon_scope"
    xmlns:views="using:silicon_scope.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="silicon-scope"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="silicon-scope" Grid.Row="0">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
            <TitleBar.Content>
                <Grid HorizontalAlignment="Stretch">
                    <ToggleButton x:Name="ProjectorModeToggle"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  IsChecked="{x:Bind ViewModel.IsProjectorMode, Mode=TwoWay}"
                                  AutomationProperties.AutomationId="ProjectorModeToggle"
                                  AutomationProperties.Name="Toggle projector mode">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE7F4;" FontSize="14" />
                            <TextBlock Text="Projector mode" />
                        </StackPanel>
                    </ToggleButton>
                </Grid>
            </TitleBar.Content>
        </TitleBar>

        <views:ProcessPickerView Grid.Row="1"
                                 x:Name="PickerView"
                                 ViewModel="{x:Bind ViewModel.Picker, Mode=OneWay}"
                                 Margin="24,16,24,8"
                                 Visibility="{x:Bind local:MainWindow.PickerVisibility(ViewModel.IsProjectorMode), Mode=OneWay}" />

        <views:ProcessLoadView Grid.Row="2"
                               x:Name="LoadView"
                               ViewModel="{x:Bind ViewModel, Mode=OneWay}"
                               Margin="24,8,24,24" />
    </Grid>
</Window>
